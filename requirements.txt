# Python deps for the AgentLevy core: cert primitives, LLM stack,
# Hedera HCS audit anchor, tests. Base + x402 settlement runs in the
# Node sub-project under web/ via @coinbase/cdp-sdk and
# @coinbase/onchainkit.
pydantic>=2.0
cryptography>=42.0
python-dotenv>=1.0
httpx>=0.27
click>=8.0
anthropic>=0.40.0
hiero-sdk-python>=0.2.6
pytest>=8.0
